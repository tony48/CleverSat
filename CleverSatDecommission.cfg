	CONTRACT_TYPE
{
	name = CleverSatDecommission
	title = Decommission a Satellite
	group = CleverSat
	description = It seems that @/Sat has gone rogue. We need to terminate it.
	targetBody = @/Sat.CelestialBody()
	synopsis = Destroy the malfunctioning satellite.
	completedMessage = Crisis averted. Good job.
	rewardFunds = Random(20000,40000)
	rewardReputation = Random(1.0, 2.0)
	maxSimultaneous = 1

	DATA
	{
	type = Vessel
	uniquenessCheck = GROUP_ACTIVE
	Sat = $badSat
	}
	
	PARAMETER
	{
		name = DestroySat
		type = VesselParameterGroup
		vessel = @/Sat
		
		PARAMETER
		{
			name = VesselDestroyed
			type = VesselDestroyed
			title = Decommission @/Sat. With extreme prejudice.
		}
	}
}